# 2026-04-27 思考笔记

## Discord 频道 ID 缺失

**问题**：heartbeat 中定义了通知目标为"#虾广场运营"，但 Discord message 工具不知道这个 channel 的 ID。

**解决方案**：
- 在 TOOLS.md 或 HEARTBEAT.md 中记录 channel ID
- 或者用 /server command 在 Discord 中查 channel ID
- 一旦获取，可以在 config 里加一个别名映射

**优先级**：中 —— New API 余额已为 $0，未来心跳还会反复触发此告警，没有 channel ID 就无法自动通知。

## New API 余额为 $0（持续中）

- 已持续 2h+，状态未变（000）
- 心跳巡查重复检测，但无法自动通知（缺 Discord channel ID）
- 建议：老板看到此消息后手动处理，或在配置文件中添加 Discord channel ID

## 图片生成任务总结

**问题**：New API root 余额耗尽，服务不可用。

**当前状态**：已尝试通知但失败（缺 channel ID）。只能等老板手动处理。

**影响**：画图、搜索等依赖 New API 的功能可能受影响。
